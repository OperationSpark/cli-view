# node-view
A Node.js module, a utility supporting cli menu and table creation to assist in creating simple cli games and apps...

node-view wraps the popular node packages of prompt and cli-table to provide a simple interface for quickly creating cli menues and tables.

###Installtion

From the command-line, execute:

    $ npm install --save node-view

Usage:

